package examen;

import java.util.Arrays;

public class TiposArrays {

	public static void main(String[] args) {
		
		  
		
				/*
				  Importar el arrays —
				 
				Arrays.toString() -Sirve para mostrar el contenido del array
				Arrays.sort() -Ordena los elementos de un array de menor a mayor (orden ascendente)
				Arrays.deepToString(array) -Como toString(), pero para arrays multidimensionales (por ejemplo, matrices).
				Arrays.fill(array, valor) -Rellena todo el array con el mismo valor.
				Arrays.equals(array1, array2) -Compara si dos arrays tienen los mismos valores y en el mismo orden
				Arrays.copyOf(array, nuevoTamaño) -Crea una copia del array con el tamaño que quieras.
				
				*/


	}

}
